# 个人导航站 MVP 需求规格说明书

> **版本**: 1.0
> **日期**: 2025-10-31
> **定位**: 轻量级、可插拔、纯前端个人信息管理导航站

---

## 🎯 项目概述

### 核心定位
构建一个**轻量级、可插拔、纯前端**的个人导航站，主打个人信息管理，支持简单认证和向量存储。

### 设计理念
- **轻量级**: 单文件部署，开箱即用
- **可插拔**: 模块化设计，功能按需组合
- **零依赖**: 纯前端实现，无服务器要求
- **智能化**: 向量存储支持语义搜索

---

## 📋 MVP核心功能

### 1. 基础导航功能 (必需)

#### 1.1 网址导航
- [x] **网址卡片展示**: 网站图标、标题、描述
- [x] **分类管理**: 文件夹式分类组织
- [x] **快速搜索**: 实时搜索过滤
- [x] **响应式布局**: 支持桌面和移动端

#### 1.2 书签管理
- [x] **添加书签**: 手动添加网址信息
- [x] **编辑功能**: 修改标题、描述、分类
- [x] **删除功能**: 移除不需要的书签
- [x] **排序功能**: 拖拽或手动排序

### 2. 数据管理功能 (必需)

#### 2.1 本地存储
- [x] **localStorage**: 基础配置和分类数据
- [x] **IndexedDB**: 书签详情和向量数据
- [x] **数据持久化**: 自动保存，刷新不丢失

#### 2.2 导入导出
- [x] **Chrome书签导入**: 解析浏览器书签文件
- [x] **JSON导出**: 备份个人数据
- [x] **批量操作**: 支持批量导入导出

### 3. 认证系统 (可选)

#### 3.1 密码保护
- [x] **简易密码认证**: 本地密码验证
- [x] **会话管理**: 登录状态保持
- [x] **密码重置**: 通过数据文件重置

#### 3.2 隐私保护
- [x] **本地加密**: 敏感数据本地加密存储
- [x] **隐私模式**: 一键隐藏敏感信息

### 4. 搜索系统 (必需)

#### 4.1 基础搜索
- [x] **关键词搜索**: 标题、描述、URL匹配
- [x] **分类过滤**: 按分类筛选结果
- [x] **搜索历史**: 记录搜索关键词

#### 4.2 智能搜索 (可选)
- [x] **模糊匹配**: 支持拼写错误容错
- [x] **标签搜索**: 基于标签的精确查找

---

## 🔧 技术架构

### 前端技术栈
```json
{
  "framework": "Vue3",
  "language": "TypeScript",
  "builder": "Vite",
  "ui": "Naive UI",
  "state": "Pinia",
  "storage": "localStorage + IndexedDB",
  "search": "Fuse.js",
  "deploy": "静态文件托管"
}
```

### 数据结构设计

#### 1. 书签数据模型
```typescript
interface Bookmark {
  id: string
  title: string
  url: string
  description?: string
  favicon?: string
  categoryId: string
  tags: string[]
  createdAt: Date
  updatedAt: Date
  isPrivate: boolean
  clickCount: number
  lastVisited?: Date
}
```

#### 2. 分类数据模型
```typescript
interface Category {
  id: string
  name: string
  icon?: string
  color?: string
  parentId?: string
  sort: number
  isPrivate: boolean
}
```

#### 3. 配置数据模型
```typescript
interface AppConfig {
  theme: 'light' | 'dark' | 'auto'
  language: string
  searchEngine: string
  passwordEnabled: boolean
  autoBackup: boolean
  maxBookmarksPerPage: number
}
```

### 存储架构

#### localStorage (轻量数据)
```typescript
// 配置和小数据存储
interface LocalStorageData {
  config: AppConfig
  categories: Category[]
  session: SessionData
}
```

#### IndexedDB (重量数据)
```typescript
// 书签和向量数据存储
interface IndexedDBData {
  bookmarks: Bookmark[]
  searchIndex: SearchIndexData
  backupHistory: BackupData[]
}
```

---

## 🔌 可插拔架构设计

### 插件系统架构

```typescript
interface Plugin {
  name: string
  version: string
  description: string
  author: string
  enabled: boolean
  config: PluginConfig
  install: () => void
  uninstall: () => void
  activate: () => void
  deactivate: () => void
}
```

### 核心插件列表

#### 1. 书签管理插件 (内置)
```typescript
const bookmarkPlugin: Plugin = {
  name: 'bookmark-manager',
  version: '1.0.0',
  description: '书签管理核心功能',
  enabled: true,
  config: {
    maxBookmarks: 10000,
    enableFavicon: true,
    enableScreenshots: false
  }
}
```

#### 2. 搜索插件 (内置)
```typescript
const searchPlugin: Plugin = {
  name: 'search-engine',
  version: '1.0.0',
  description: '搜索引擎功能',
  enabled: true,
  config: {
    searchEngine: 'fuse',
    enableFuzzy: true,
    threshold: 0.3
  }
}
```

#### 3. 认证插件 (可选)
```typescript
const authPlugin: Plugin = {
  name: 'password-auth',
  version: '1.0.0',
  description: '密码认证功能',
  enabled: false,
  config: {
    passwordHash: '',
    sessionTimeout: 3600,
    maxAttempts: 5
  }
}
```

#### 4. 主题插件 (内置)
```typescript
const themePlugin: Plugin = {
  name: 'theme-system',
  version: '1.0.0',
  description: '主题和样式系统',
  enabled: true,
  config: {
    defaultTheme: 'auto',
    customColors: {},
    enableAnimations: true
  }
}
```

### 插件管理接口

```typescript
interface PluginManager {
  // 插件生命周期
  installPlugin(plugin: Plugin): Promise<boolean>
  uninstallPlugin(pluginName: string): Promise<boolean>
  enablePlugin(pluginName: string): boolean
  disablePlugin(pluginName: string): boolean

  // 插件状态管理
  getPluginStatus(pluginName: string): PluginStatus
  listPlugins(): Plugin[]
  updatePlugin(pluginName: string, update: Partial<Plugin>): boolean

  // 插件通信
  emit(event: string, data: any): void
  on(event: string, handler: Function): void
  off(event: string, handler: Function): void
}
```

---

## 🎨 UI/UX设计

### 设计原则
- **简洁至上**: 减少视觉干扰，突出核心功能
- **响应式设计**: 适配各种屏幕尺寸
- **无障碍访问**: 支持键盘导航和屏幕阅读器
- **性能优先**: 快速加载，流畅交互

### 页面结构
```
┌─────────────────────────────────────────┐
│                Header                   │
│  Logo | Search | Theme | Settings      │
├─────────────────────────────────────────┤
│                Sidebar                 │
│  Categories                            │
│  ├ All Bookmarks                       │
│  ├ Development                         │
│  ├ Design                              │
│  └ ...                                 │
├─────────────────────────────────────────┤
│              Main Content              │
│  ┌─────────────┐ ┌─────────────┐       │
│  │   Card 1    │ │   Card 2    │       │
│  │   Website   │ │   Website   │       │
│  └─────────────┘ └─────────────┘       │
└─────────────────────────────────────────┘
```

### 组件层次
```
App
├── TheHeader
│   ├── Logo
│   ├── SearchBox
│   ├── ThemeToggle
│   └── UserMenu
├── TheSidebar
│   ├── CategoryList
│   ├── AddCategoryBtn
│   └── PluginPanel
├── TheMain
│   ├── BookmarkGrid
│   │   └── BookmarkCard
│   ├── EmptyState
│   └── LoadingState
└── TheFooter
    ├── Statistics
    └── VersionInfo
```

---

## 📊 数据流设计

### 状态管理 (Pinia)

#### 1. 书签状态
```typescript
export const useBookmarkStore = defineStore('bookmark', {
  state: () => ({
    bookmarks: [] as Bookmark[],
    categories: [] as Category[],
    selectedCategory: 'all',
    searchQuery: '',
    loading: false
  }),

  getters: {
    filteredBookmarks: (state) => {
      // 搜索和分类过滤逻辑
    },

    categorizedBookmarks: (state) => {
      // 按分类分组逻辑
    }
  },

  actions: {
    async addBookmark(bookmark: Omit<Bookmark, 'id'>) {},
    async updateBookmark(id: string, updates: Partial<Bookmark>) {},
    async deleteBookmark(id: string) {},
    async importBookmarks(file: File) {},
    async exportBookmarks() {}
  }
})
```

#### 2. 配置状态
```typescript
export const useConfigStore = defineStore('config', {
  state: () => ({
    config: defaultConfig,
    plugins: [] as Plugin[],
    theme: 'auto',
    authenticated: false
  }),

  actions: {
    updateConfig(updates: Partial<AppConfig>) {},
    togglePlugin(pluginName: string) {},
    setTheme(theme: string) {},
    authenticate(password: string) {}
  }
})
```

### 数据持久化策略

#### 1. 自动保存
```typescript
// 防抖自动保存
const autoSave = useDebounceFn(() => {
  saveToLocalStorage()
  saveToIndexedDB()
}, 1000)

// 监听状态变化
watch(
  () => bookmarkStore.$state,
  autoSave,
  { deep: true }
)
```

#### 2. 数据同步
```typescript
// 多标签页同步
window.addEventListener('storage', (e) => {
  if (e.key === 'bookmark-data') {
    bookmarkStore.loadFromStorage()
  }
})
```

---

## 🔍 搜索功能设计

### 搜索架构
```typescript
interface SearchEngine {
  // 基础搜索
  search(query: string): Promise<SearchResult[]>

  // 高级搜索
  fuzzySearch(query: string): Promise<SearchResult[]>
  semanticSearch(query: string): Promise<SearchResult[]>

  // 索引管理
  buildIndex(bookmarks: Bookmark[]): Promise<void>
  updateIndex(bookmark: Bookmark): Promise<void>
  clearIndex(): Promise<void>
}
```

### Fuse.js 集成
```typescript
import Fuse from 'fuse.js'

const fuseOptions = {
  keys: [
    { name: 'title', weight: 0.7 },
    { name: 'description', weight: 0.2 },
    { name: 'tags', weight: 0.1 },
    { name: 'url', weight: 0.1 }
  ],
  threshold: 0.3,
  includeScore: true,
  includeMatches: true,
  minMatchCharLength: 2,
  useExtendedSearch: true
}

export class FuseSearchEngine implements SearchEngine {
  private fuse: Fuse<Bookmark>

  constructor(bookmarks: Bookmark[]) {
    this.fuse = new Fuse(bookmarks, fuseOptions)
  }

  async search(query: string): Promise<SearchResult[]> {
    const results = this.fuse.search(query)
    return results.map(result => ({
      bookmark: result.item,
      score: result.score || 0,
      matches: result.matches
    }))
  }
}
```

---

## 🚀 部署方案

### 静态部署选项

#### 1. GitHub Pages (推荐)
```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

#### 2. Vercel (备选)
- 自动部署 GitHub 仓库
- 自定义域名支持
- 边缘函数支持 (后续扩展)

#### 3. Netlify (备选)
- 拖拽部署
- 表单处理 (未来功能)
- 分支预览

### 配置优化

#### 1. Vite 配置
```typescript
// vite.config.ts
export default defineConfig({
  plugins: [vue()],
  build: {
    target: 'es2015',
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router', 'pinia'],
          ui: ['naive-ui'],
          utils: ['fuse.js', 'dayjs']
        }
      }
    }
  },
  base: './' // GitHub Pages 兼容
})
```

#### 2. PWA 支持 (可选)
```typescript
// vite.config.ts
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}']
      }
    })
  ]
})
```

---

## 📋 开发计划

### Phase 1: 核心架构 (第1周)
- [x] 项目初始化 (Vite + Vue3 + TypeScript)
- [x] 基础路由和页面结构
- [x] 状态管理配置 (Pinia)
- [x] 插件系统框架搭建
- [x] 基础UI组件集成

### Phase 2: 基础功能 (第2周)
- [x] 书签CRUD功能实现
- [x] 分类管理系统
- [x] 本地存储集成
- [x] 基础搜索功能
- [x] 响应式布局

### Phase 3: 高级功能 (第3周)
- [x] Chrome书签导入导出
- [x] 密码认证系统
- [x] 主题切换功能
- [x] 搜索优化 (Fuse.js)
- [x] 数据备份恢复

### Phase 4: 优化完善 (第4周)
- [x] 性能优化
- [x] 错误处理
- [x] 用户体验优化
- [x] 文档编写
- [x] 部署配置

---

## 🎯 成功指标

### 性能指标
- **首次加载时间**: < 2秒
- **搜索响应时间**: < 200ms
- **内存占用**: < 50MB
- **包体积**: < 1MB (gzipped)

### 功能指标
- **书签容量**: 支持 10,000+ 书签
- **搜索准确性**: 95%+ 相关度
- **数据完整性**: 100% 不丢失
- **浏览器兼容**: 支持现代浏览器

### 用户体验指标
- **易用性**: 5分钟内上手
- **可访问性**: WCAG 2.1 AA标准
- **响应式**: 完美适配移动端
- **国际化**: 支持中英文

---

## 📚 技术选型说明

### 为什么选择 Vue3?
- 渐进式框架，学习成本低
- Composition API 适合复杂逻辑
- 生态丰富，社区活跃
- TypeScript 支持良好

### 为什么选择 Fuse.js?
- 轻量级，无依赖
- 模糊搜索功能强大
- Vue 生态集成好
- 配置灵活

### 为什么选择 IndexedDB?
- 浏览器原生支持
- 存储容量大 (GB级)
- 支持事务和索引
- 异步操作不阻塞

### 为什么选择插件架构?
- 功能可插拔，按需启用
- 便于扩展和维护
- 降低核心复杂度
- 支持第三方插件

---

**文档版本**: 1.0
**最后更新**: 2025-10-31
**审核状态**: 待审核
**下一步**: 开始技术选型和项目初始化